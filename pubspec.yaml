name: three_question_data_model
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.3.2
  pedantic_mono: ^1.20.0

flutter:
  uses-material-design: true
